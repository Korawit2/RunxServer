export interface ObjectSort {
    [key: string]: string | number | object;
}
export interface ExcelUploadRuner {
    rank: string
    time: string,
    firstname: string,
    lastname: string,
    gender: string
    age_group: string
    nationality: string
}